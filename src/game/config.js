game.config = {
  name: 'Mad Camera',
  version: '0.0.1',

  system: {
    width: 400,
    height: 300,

    /* Scale and center to fit window on desktop */
    center: true,
    scale: true,
    scaleToFit: true,

    /* Resize on mobile */
    resizeToFill: true,

    webGL: false
  }
};
